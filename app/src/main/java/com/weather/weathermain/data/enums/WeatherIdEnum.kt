package com.weather.weathermain.data.enums

import android.support.annotation.StringRes
import com.weather.weathermain.R

enum class WeatherIdEnum(val weatherId: Int, val weatherName: String) {
    ARR(500, "Clear Sky")
}